# ============================================
# 📈 PROMETHEUS CONFIGURATION - DESARROLLO
# Configuración para monitoreo de métricas
# ============================================

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  # ============================================
  # 🎯 PROMETHEUS SELF-MONITORING
  # ============================================
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
    scrape_interval: 30s
    metrics_path: /metrics

  # ============================================
  # 🚀 LUZZIA BACKEND - Métricas de aplicación
  # ============================================
  - job_name: 'luzzia-backend'
    static_configs:
      - targets: ['luzzia-backend:4000']
    scrape_interval: 15s
    metrics_path: /api/v1/metrics
    scrape_timeout: 10s
    honor_labels: true
    params:
      format: ['prometheus']

  # ============================================
  # 🔴 REDIS EXPORTER - Métricas de Redis
  # ============================================
  - job_name: 'redis'
    static_configs:
      - targets: ['redis:6379']
    scrape_interval: 30s
    metrics_path: /metrics

  # ============================================
  # 🍃 MONGODB EXPORTER - Métricas de MongoDB
  # ============================================
  - job_name: 'mongodb'
    static_configs:
      - targets: ['mongodb:27017']
    scrape_interval: 30s
    metrics_path: /metrics

  # ============================================
  # 🐳 DOCKER - Métricas de contenedores (opcional)
  # ============================================
  # - job_name: 'docker'
  #   static_configs:
  #     - targets: ['docker-exporter:9323']
  #   scrape_interval: 30s

# ============================================
# 🔔 ALERTING CONFIGURATION
# ============================================
alerting:
  alertmanagers:
    - static_configs:
        - targets:
          # - alertmanager:9093

# ============================================
# 📊 REMOTE WRITE/READ (para producción)
# ============================================
# remote_write:
#   - url: "http://your-remote-prometheus/api/v1/write"
# 
# remote_read:
#   - url: "http://your-remote-prometheus/api/v1/read"